import { StyleSheet, } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 28,
        fontWeight: '700',
        textAlign: 'left',
        color: 'black',
        paddingTop: 24,
        paddingBottom: 24,
    },

})

export default styles;
