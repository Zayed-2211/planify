import { StyleSheet, } from "react-native";

const styles = StyleSheet.create({
    drawer: {
        margin: 22
    }, 
    link: {
        fontSize: 16,
        fontWeight: '500',
        marginVertical: 14,
        color: '#000000'
    }
})

export default styles;
